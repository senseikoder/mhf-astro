---
export const prerender = false;

const wpurl = import.meta.env.WP_URL;
const res = await fetch(
   `${wpurl}/posts?per_page=5&lang=es`,
);

const posts = await res.json();
---

<div class="title is-5">Últimas Entradas</div>
<ul class="ml-4">
   {
      posts.map((post: any) => (
         <li class="mb-4">
            <a href={`/blog/${post.slug}`}>
               <span class="has-text-info-40">{post.title.rendered}</span>
            </a>
         </li>
      ))
   }
</ul>

<style>
   ul {
      list-style: none;
   }
</style>
